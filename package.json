{
  "name": "gunslol",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.6"
  }
}
